package common;

public class Commands {
    public enum Command {
        CONNECT,
        DISCONNECT,
        START_GAME,
        GUESS_LETTER,
        GUESS_WORD,
        UNKNOWN
    }
}
